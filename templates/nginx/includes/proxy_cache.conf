proxy_cache_path {{ subitem.proxy_cache_path | default('/var/cache/nginx', true) }} levels={{ subitem.levels | default('1:2', true) }} keys_zone={{ subitem.keys_zone | default('web_cache', true) }}:{{ subitem.size | default('20m', true) }} max_size={{ subitem.max_size | default('200m', true) }} inactive={{ subitem.inactive | default('30m', true) }};