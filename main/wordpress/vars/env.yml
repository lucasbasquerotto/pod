{% if params.env_files | default([]) | length == 0 %}  
env_files: []
{% else %} 
env_files:
{% for item in params.env_files %}  
- src: "{{ item.src }}"
  dest: "{{ item.dest }}"
  mode: "{{ item.mode | default('', true) }}"
{% endfor %}
{% endif %}