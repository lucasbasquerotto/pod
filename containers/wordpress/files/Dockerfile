ARG WORDPRESS_IMAGE
ARG WORDPRESS_VERSION

FROM $WORDPRESS_IMAGE:$WORDPRESS_VERSION

ARG POD_ENV_DIR
ARG WORDPRESS_INI_FILE_TYPE

COPY --chown=www-data:www-data $POD_ENV_DIR/wordpress/.env /var/www/html/

RUN mv "$PHP_INI_DIR/php.ini-$WORDPRESS_INI_FILE_TYPE" "$PHP_INI_DIR/php.ini" 
